# Skills

Place shareable or open-source skills under `<category>/<skill-name>/` here.
Reusable scaffolds live in the repository's top-level `templates/` directory, outside the catalog.

Each skill should have:

- `skill.json`
- `README.md`
- optional `tests/`
- optional `examples/`
