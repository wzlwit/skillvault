# Rules Core

Global engineering guidance for evidence-based, scoped, pragmatic coding work. The source
version may contain longer explanations and examples; this skill contains only the four core
rules. Global installation makes it available across projects; its instructions load when
invoked or selected, not automatically in every session. For always-on rules, use the host's
global or project instructions explicitly, without duplicating conflicting rule documents.