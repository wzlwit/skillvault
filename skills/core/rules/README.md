# Rules

Use `/rules <add|modify|remove> <rule> [location]` to propose and, after confirmation, apply
narrowly scoped changes to an authoritative AI-rules document. It synchronizes `rules`
only when the change belongs in its four core rules.